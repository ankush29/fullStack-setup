{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "proxy": "http://localhost:3030/",
  "scripts": {
    "restart": "pm2 startOrRestart ecosystem.config.js",
    "deploy": "pm2 deploy ecosystem.config.js production",
    "start": "NODE_ENV=development concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "build": "NODE_ENV=production react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "client": "PORT=8001 react-scripts start",
    "server": "nodemon server.js"
  },
  "author": "Ankush",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "date-fns": "^1.30.1",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.2"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "pm2": "^3.2.7",
    "concurrently": "^4.0.1",
    "nodemon": "^1.17.2"
  }
}
